# Supabase migrations

Owned by `scoop-app`.

## Status (Phase 6A.4)

No executable migration yet.

- **Specification:** Phase 6A.2 canonical data schema (in `scoop-protocol` docs)
- **First migration:** created after local Supabase project / env validation
- **Do not** link a production Supabase project during bootstrap

This directory exists so tooling and future `supabase db` commands have a stable home.
