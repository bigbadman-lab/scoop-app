import { HELLO_FIXTURE } from '@scoop/shared';

/** Canonical HELLO fixture for the indexer vertical slice (mirrors @scoop/shared). */
export const HELLO = HELLO_FIXTURE;

export const HELLO_BLOCK = HELLO.blockNumber;
export const HELLO_TX_HASH = HELLO.txHash;
export const HELLO_STREAM = HELLO.streamName;
